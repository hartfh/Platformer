var Asset = function(config) {
	var config = config || {};

	this.nicename	= config.nicename;
	this.handle	= config.handle;
	this.node		= config.node;
	//this.color;

	// other properties? solidity, affected by gravity (mass)
};
